import Foundation

public enum KeychainKeys {
    public enum Auth {
        public static let accessToken = "ToDoList-AccessToken"
        public static let location = "ToDoList-Location"
    }
    
    public enum User {
        public static let profile = "ToDoList"
    }
}
